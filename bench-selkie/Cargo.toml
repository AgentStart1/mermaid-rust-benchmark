[package]
name = "bench-selkie"
version = "0.1.0"
edition = "2021"

[dependencies]
selkie-rs = "0.3"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false
