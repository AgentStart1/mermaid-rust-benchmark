[package]
name = "bench-mermaid-rs-renderer"
version = "0.1.0"
edition = "2021"

[dependencies]
mermaid-rs-renderer = "0.2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false
